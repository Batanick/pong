#version 330 core

layout (location = 0) in vec3 vertexPosition;
 
out vec2 pos;

void main(){
   gl_Position = vec4( vertexPosition, 1 );
   pos = vec2( (vertexPosition.x + 1) / 2,  (vertexPosition.y + 1) / 2 );
}