#version 330 core

layout (location = 0) in vec3 vertexPosition;

uniform mat4 mvp;
uniform float heightScale;

out float height;

void main() {
	gl_Position = mvp * vec4(vertexPosition, 1);
	height = vertexPosition.y / heightScale ;
}